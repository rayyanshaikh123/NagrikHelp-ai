fastapi==0.115.0
uvicorn[standard]==0.30.6
transformers==4.44.2
Pillow==10.4.0
datasets==2.21.0
uvicorn
fastapi
Pillow
transformers
# Pin CPU PyTorch wheels so Render can install prebuilt binaries from the
# official PyTorch wheel index. If you need CUDA, replace these with the
# appropriate CUDA wheel spec from https://pytorch.org/get-started/locally/.
torch==2.2.0+cpu
torchvision==0.18.1+cpu
torchaudio==2.3.2+cpu