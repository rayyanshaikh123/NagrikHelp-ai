fastapi==0.115.0
uvicorn[standard]==0.30.6
transformers==4.44.2
Pillow==10.4.0
datasets==2.21.0
uvicorn
fastapi
Pillow
transformers
# Pin a CPU torch wheel; Render buildCommand will install this wheel from the
# official PyTorch index which speeds builds and avoids source compilation.
torch==2.2.0+cpu --index-url https://download.pytorch.org/whl/cpu