fastapi==0.115.0
uvicorn[standard]==0.30.6
transformers==4.44.2
Pillow==10.4.0
datasets==2.21.0
uvicorn
fastapi
Pillow
transformers
# NOTE: `torch` is installed in `render.yaml` buildCommand from the
# PyTorch wheel index (using --extra-index-url). Leaving `torch` out of the
# requirements file avoids pip attempting to resolve a `+cpu` local-version
# suffix on PyPI which can cause "No matching distribution" errors.