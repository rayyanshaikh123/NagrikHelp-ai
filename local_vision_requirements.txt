fastapi==0.115.0
uvicorn[standard]==0.30.6
transformers==4.44.2
Pillow==10.4.0
datasets==2.21.0
uvicorn
fastapi
Pillow
transformers
# Install stable PyTorch packages. The Render build uses an extra PyTorch
# wheel index to prefer prebuilt binaries when available; during local
# development on macOS you may need to follow https://pytorch.org/get-started/locally/
# to install a platform-appropriate wheel.
torch==2.2.0
torchvision==0.18.1
torchaudio==2.3.2