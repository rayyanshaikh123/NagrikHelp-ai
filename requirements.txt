# Production deployment requirements for NagrikHelp AI Server
# Optimized for headless Linux environments (Render, Railway, Docker, etc.)

fastapi==0.115.0
uvicorn[standard]==0.30.6
transformers==4.44.2
Pillow==10.4.0
datasets==2.21.0

# NumPy - install before torch
numpy>=1.24,<2.0

# PyTorch - CPU version for deployment (smaller, faster on cloud)
torch==2.9.0
torchvision==0.24.0

# YOLOv8 for object detection
ultralytics>=8.0.0

# OpenCV HEADLESS - for servers without display/GUI
# This version doesn't require libGL, libGTK, etc.
opencv-python-headless>=4.8.0

# Python dotenv for environment variables
python-dotenv>=1.0.0
